下午成功的完成了你问我答的整体代码，但是最后却遇到了接口调用的未知问题，那就再等明天看看接口到底是不是还好。
今天下午在过滤注入的时候发现了一个很好的函数，在碰到sql语句需要传入数字的时候可以用这个函数进行过滤，来检测传入的是否是数字，如果不是就返回传入非法参数的错误，这样子在某些方面应用是特别棒的。
这个函数介绍如下：
is_numeric (PHP 4, PHP 5)
is_numeric — 检测变量是否为数字或数字字符串
描述
bool is_numeric ( mixed $var )
如果 var 是数字和数字字符串则返回 TRUE，否则返回 FALSE。
参见 is_bool()、is_float()、is_int()、is_string()、is_object()、is_array() 和 is_integer()。
算是今天额外的收获吧。
晚上的时候并不想去做什么事情，下午写代码也是很累了，但是还是觉得需要做其它的东西来充实自己。
或许我该去做一些其他的有意义的东西吧，这个是需要慢慢思考的。
更新不止，创造不息。
BY 夜雨、loverchen96